# ASP-.NET-learning
A project to learn ASP .NET API development with simple CRUD operations

Start the MySQL 8 container for the project
```bash
# Start the MySQL service
docker-compose up -d

# Stop it when done
docker-compose down
```
